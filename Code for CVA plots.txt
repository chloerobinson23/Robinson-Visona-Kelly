# All whales CVA plots
# Final scripts used in paper
# [January 4, 2025]


#######################
## All whales
#######################


library(stringr) # str_split
library(reshape2) # dcast
library(ggplot2) # ggplot
library(data.table) # setDT
library(plyr) # ddply
library(gridExtra)

###import CV scores for 6LM 2 photos for candidate whales

cva<- read.csv(("CVA scores_candidate whales_2025.csv"), stringsAsFactors =TRUE, na.strings = c(""))
attach(cva)
str(cva)

# Plot1 - color by pregnancy status

chulls.preg <- ddply(cva, .(status), function(cva) cva[chull(cva$CV1, cva$CV2), ])

p1 <- ggplot(data=cva, aes(x=CV1, y=CV2)) + 
  geom_polygon(data=chulls.preg, aes(x=CV1, y=CV2, fill=status), alpha=0.5, show.legend = FALSE) +
  geom_point(data=cva, aes(color=status), size=4) +
  theme_bw() +
  xlab("CV1 (50.4%)")+
  ylab("CV2 (39.5%)")+
  labs(color="Reproductive Status")+
  scale_color_viridis_d(option = "viridis")+
  scale_fill_viridis_d(option = "viridis")+
  theme(
    plot.title = element_text(size=10),
    axis.text.x = element_text(hjust=1),
    panel.grid.major = element_blank(), 
    panel.grid.minor = element_blank(),
    panel.background = element_blank(), 
    axis.line = element_line(colour = "black"),
    legend.key=element_blank(),
    axis.title = element_text(size=12),
    axis.text = element_text(size=12),
    axis.title.x = element_text(size = 14, face = "bold"),
    axis.title.y = element_text(size = 14, face = "bold"),
    legend.title = element_text(size=12, face = "bold"),
    legend.text = element_text(size=12))


p1

ggsave("Figure S3_CVA_preg status.tiff", units = "in", width = 10, height = 7, dpi = 300, p1)



###import CV scores for 6LM 2 photos for test whales

cva2<- read.csv(("CVA scores_test whales_2025.csv"), stringsAsFactors =TRUE, na.strings = c(""))
attach(cva2)
str(cva2)

# Plot2 - color by reproductive status

chulls.preg <- ddply(cva2, .(status), function(cva2) cva2[chull(cva2$CV1, cva2$CV2), ])

p2 <- ggplot(data=cva2, aes(x=CV1, y=CV2)) + 
  geom_polygon(data=chulls.preg, aes(x=CV1, y=CV2, fill=status), alpha=0.5, show.legend = FALSE) +
  geom_point(data=cva2, aes(color=status), size=4) +
  theme_bw() +
  xlab("CV1 (66.7%)")+
  ylab("CV2 (22.1%)")+
  labs(color="Reproductive Status")+
  scale_color_viridis_d(option = "viridis")+
  scale_fill_viridis_d(option = "viridis")+
  theme(
    plot.title = element_text(size=10),
    axis.text.x = element_text(hjust=1),
    panel.grid.major = element_blank(), 
    panel.grid.minor = element_blank(),
    panel.background = element_blank(), 
    axis.line = element_line(colour = "black"),
    legend.key=element_blank(),
    axis.title = element_text(size=12),
    axis.text = element_text(size=12),
    axis.title.x = element_text(size = 14, face = "bold"),
    axis.title.y = element_text(size = 14, face = "bold"),
    legend.title = element_text(size=12, face = "bold"),
    legend.text = element_text(size=12))


p2

ggsave("Figure3_CVA_pop preg status.tiff", units = "in", width = 10, height = 7, dpi = 300, p2)

### END ###

